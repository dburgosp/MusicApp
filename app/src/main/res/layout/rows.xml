<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent">
    <LinearLayout
        android:id="@+id/row_list_element"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <ImageView
            android:id="@+id/row_list_element_image"
            android:layout_width="@dimen/image"
            android:layout_height="@dimen/image"
            android:background="@drawable/shadow_layer"
            android:padding="4dp" />

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="@dimen/general_padding">

            <TextView
                android:id="@+id/row_list_element_title"
                style="@style/ListElementTitle"
                android:text="Title" />

            <TextView
                android:id="@+id/row_list_element_subtitle"
                style="@style/ListElementSubtitle"
                android:text="Subtitle" />
        </LinearLayout>
    </LinearLayout>

    <View
        style="@style/Separator"
        android:layout_below="@id/row_list_element" />
</LinearLayout>